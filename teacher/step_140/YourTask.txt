Ментор обратил ваше внимание на то, что в коде приложения не
используются транзакции к базе данных. Приложение сохраняет данные,
но нигде в приложении нет управления транзакциями. Все запросы в
базу данных идут в рамках отдельных транзакций. Так делать нельзя.
Все данные должны сохраняться в рамках одной транзакции.

Ваша задача:
1. Узнать, что такое декларативное управление транзакциями;
2. Узнать, как применить декларативное управление транзакциями в Spring приложении;
3. Переключить приложение на реальную базу данных;
4. Замерить +/- скорость выполнения запросов (она пишется в лог) до
   применения декларативного управления транзакциями;
5. Применить декларативное управление транзакциями для TravelCalculatePremiumServiceImpl;
6. Замерить +/- скорость выполнения запросов (она пишется в лог) после
   применения декларативного управления транзакциями.

PS: время выполнения запросов должно уменьшиться
(это будет видно при работе приложения с реальным сервером базы
данных, а не с in memory H2). Почему?
