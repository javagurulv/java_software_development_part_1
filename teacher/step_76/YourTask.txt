Ментор предлагает воспользоваться классом ClassifierValueRepository
и его методом findByClassifierTitleAndIc(...) для создания
новой валидации. Новая валидация должна проверять значения selected_risks
в запросе. Все значения указанные в selected_risks должны
быть значениями ic (ClassifierValue.ic) классификатора RISK_TYPE
из базы данных. Для каждого не верного значения в selected_risks
валидация должна возвращать ошибку:
Риск с ic = NOT_EXISTING_RISK не поддерживается системой.

Перед тем как читать дальше: проанализируйте можно ли с текущей
реализацией создать валидацию, которая бы возвращала не одну ошибку,
а список ошибок?

interface TravelRequestValidation {

    Optional<ValidationError> execute(TravelCalculatePremiumRequest request);

}

Без изменения интерфейса TravelRequestValidation это сделать невозможно.

Перед тем как читать дальше: попробуйте подумать и предложить идеи
какие изменения нужно внести в код для того, чтобы стало возможно создать
валидацию, которая бы возвращала список ошибок. Было бы хорошо
оставить поддержку двух вариантов:
- старого, когда валидация возвращает одну ошибку;
- нового, когда валидация возвращает список ошибок.

Попробуйте реализовать и обсудите ваши идеи с ментором.